/* import { ListItem } from "@material-ui/core";
import React from "react";
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link,
  useParams,
} from "react-router-dom";

function List({ lastName }) {
  const { slug } = useParams();
  return (
    <div>
      <h1>{[slug]}hello</h1>
    </div>
  );
}

export default List;
 */
